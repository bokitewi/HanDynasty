# The vanilla challenge-character database only contains historical links for
# the replaced vanilla world. Keep it empty in the Spring and Autumn total
# conversion so those dead character/title/house references are not resolved.
